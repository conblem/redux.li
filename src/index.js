const { setInitialState, setMiddlewares, setReducer } = require("./store");
const matchers = require("./matchers");

module.exports = {
  setInitialState,
  setMiddlewares,
  setReducer,
  matchers
};
