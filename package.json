{
  "name": "redux.li",
  "version": "0.1.0",
  "description": "",
  "main": "src/index.js",
  "types": "index.d.ts",
  "scripts": {
    "precommit": "lint-staged",
    "test": "jest --coverage",
    "jest": "jest --watch"
  },
  "author": "conblem",
  "license": "MIT",
  "engines": {
    "node": ">=6.0.0"
  },
  "lint-staged": {
    "*.{js,json,css,md}": ["prettier --write", "git add"]
  },
  "devDependencies": {
    "@types/jest": "^22.1.1",
    "husky": "^0.14.3",
    "jest": "^22.1.4",
    "lint-staged": "^6.1.0",
    "prettier": "^1.10.2",
    "redux-thunk": "^2.2.0"
  },
  "dependencies": {
    "redux": "^3.7.2"
  }
}
